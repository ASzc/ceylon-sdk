import ceylon.io {
    FileDescriptor
}
import ceylon.net.http {
    Message
}

shared Message receive(FileDescriptor sender) {
    // TODO old Request class called Parser(socket).parseResponse(), anything reusable?
    
    Message incoming = nothing;
    
    return incoming;
}
